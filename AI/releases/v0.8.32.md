# TestMe v0.8.32 Release Notes

Released: 2025-12-10

## Enhancements

### --clean Runs Cleanup Service Scripts

The `--clean` flag now runs cleanup service scripts defined in `testme.json5` configurations before removing `.testme` artifact directories.

**Previous Behavior:**

`tm --clean` only removed `.testme` artifact directories containing compiled binaries and build logs.

**New Behavior:**

`tm --clean` now:

1. Recursively finds all `testme.json5` files in the directory tree
2. Runs each config's cleanup script (if defined)
3. Removes `.testme` artifact directories

**Use Cases:**

- Clean up test-generated files (temp files, logs, uploads)
- Kill background processes that tests may have started
- Reset test environment to pristine state
- Prepare for fresh test runs after debugging

**Example:**

With a `testme.json5` containing:

```json5
{
    services: {
        cleanup: 'cleanup.sh',
    },
}
```

And a `cleanup.sh` that removes temp files:

```bash
#!/bin/bash
rm -rf tmp/*
rm -f *.log
```

Running `tm --clean` now produces:

```
Cleaning test artifacts...
✓ Cleanup completed successfully: cleanup.sh
✓ All test artifacts cleaned
```

**Implementation Files:**

- [src/config.ts:947-993](src/config.ts#L947-L993) - Added `findAllConfigDirs()` method
- [src/index.ts:1024-1032](src/index.ts#L1024-L1032) - Updated `--clean` handler

### Visual Studio Project Generation for --debug on Windows

The `--debug` flag on Windows now generates a proper Visual Studio project (`.vcxproj`) with environment variables configured for DLL loading.

**Previous Behavior:**

`tm --debug test.tst.c` on Windows used `devenv /UseEnv /DebugExe <binary>`, but `/UseEnv` only affects compilation, not the debugger environment. This caused DLLs to fail to load because PATH wasn't available to the debugger.

**New Behavior:**

`tm --debug test.tst.c` now:

1. Generates `<testname>.vcxproj` with compiler settings (include paths, library paths, etc.)
2. Generates `<testname>.vcxproj.user` with debug settings including `LocalDebuggerEnvironment` for PATH and other environment variables
3. Opens Visual Studio with the project

**Key Features:**

- Environment variables (including PATH for DLL loading) are persisted in the `.vcxproj.user` file
- Working directory is set to the test directory
- Include and library paths are properly configured
- All TESTME_* variables and user-defined environment variables are included
- No external tool required (unlike xcodegen on macOS)

**Example Output:**

```
🛠️  Preparing Visual Studio project...
📄 Source: C:\project\test\socket.tst.c
📂 Working Directory: C:\project\test
📝 Created Visual Studio project: socket.vcxproj
🔍 Found Visual Studio at: C:\Program Files\Microsoft Visual Studio\2022\Community\Common7\IDE\devenv.exe
🚀 Launching Visual Studio...
✅ Visual Studio launched

Visual Studio project 'socket' created and opened.
Project location: C:\project\test\.testme\socket\socket.vcxproj

To debug:
1. Visual Studio should be open with the project loaded
2. Build the project (Ctrl+Shift+B)
3. Set breakpoints in: C:\project\test\socket.tst.c
4. Start debugging (F5)

Note: Environment variables including PATH are configured in the project.
```

**Implementation Files:**

- [src/artifacts.ts:509-810](src/artifacts.ts#L509-L810) - Added `generateVisualStudioProject()`, `createVisualStudioProject()`, and helper methods
- [src/handlers/c.ts:838-964](src/handlers/c.ts#L838-L964) - Updated `launchVisualStudioDebugger()` to generate project

## Bug Fixes

### Special Variable Expansion in VS/Xcode Project Environment

Fixed environment variable expansion in Visual Studio and Xcode project generation.

**Issue:**

When generating IDE projects with `--debug`, environment variables like `${PLATFORM}`, `${PROFILE}`, `${ARCH}` were not being expanded in PATH and other environment settings.

**Example:**

```
PATH=..\build\${PLATFORM}-${PROFILE}\bin;...
```

Instead of:

```
PATH=..\build\windows-arm64-dev\bin;...
```

**Fix:**

Pass `specialVars` parameter to `GlobExpansion.expandSingle()` when expanding environment variable values in both `generateXcodeProjectConfig()` and `buildEnvironmentVariablesForMSVC()`.

**Implementation Files:**

- [src/artifacts.ts:434](src/artifacts.ts#L434) - Xcode project environment expansion
- [src/artifacts.ts:803](src/artifacts.ts#L803) - VS project environment expansion

### Windows ARM64 Platform Detection for VS Projects

Fixed Visual Studio project generation to use correct platform on Windows ARM64.

**Issue:**

VS projects were hardcoded to use `x64` platform. On Windows ARM64, Bun reports `process.arch` as `x64` due to x64 emulation, causing the build to target wrong architecture.

**Fix:**

- Detect ARM64 using `PROCESSOR_IDENTIFIER` environment variable (contains "ARM" on ARM64 systems)
- Use dynamic platform in vcxproj: `ARM64`, `x64`, or `Win32`

**Implementation Files:**

- [src/artifacts.ts:522-533](src/artifacts.ts#L522-L533) - Platform detection logic

### Library Name Preservation for VS Projects

Fixed library name handling in VS project generation to preserve the `lib` prefix.

**Issue:**

Libraries specified as `libwebsock` in config were being output as `websock.lib` instead of `libwebsock.lib`, causing linker errors when the actual files have the `lib` prefix.

**Fix:**

Changed `processLibrariesForMSVC()` to preserve library names as-is and only add `.lib` suffix if not present.

**Implementation Files:**

- [src/artifacts.ts:706-718](src/artifacts.ts#L706-L718) - Library name processing

### Node.js 18 Compatibility for Postbuild Script

Fixed `bin/postbuild.mjs` to work with Node.js 18.x on Linux.

**Issue:**

The postbuild script used `import.meta.dirname` which is only available in Bun and Node.js 20.11+. On Linux with Node.js 18.19.0, this caused the build to fail with:

```
TypeError [ERR_INVALID_ARG_TYPE]: The "paths[0]" argument must be of type string. Received undefined
```

**Fix:**

Replaced `import.meta.dirname` with the standard ESM approach using `fileURLToPath(import.meta.url)` which works across all Node.js versions that support ESM.

**Implementation Files:**

- [bin/postbuild.mjs](bin/postbuild.mjs) - Updated to use cross-platform directory resolution

## Previous Release

See [v0.8.31 Release Notes](v0.8.31.md) for the previous release.
